global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: streamforge-api
    static_configs:
      - targets: ["host.docker.internal:8000"]
    metrics_path: /metrics
