apiVersion: 1

providers:
  - name: StreamForge
    orgId: 1
    folder: StreamForge
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
