import PagePlaceholder from '../components/layout/PagePlaceholder';

/** Full-screen DAG + /topology stage docs */
export default function Pipeline() {
  return <PagePlaceholder pageId="pipeline" />;
}
