# Core streaming
confluent-kafka==2.4.0
faust-streaming==0.11.3
# faust-streaming declares aiokafka>=0.10.0 but is actually broken on 0.14.0
# (AttributeError: 'MetadataRequest_v1' object has no attribute 'prepare' —
# an internal protocol change upstream). Pin to the version confirmed working.
aiokafka==0.10.0

# State store (pure-Python RocksDB bindings — easier install than python-rocksdb)
rocksdict==0.3.19

# API & observability
fastapi==0.111.0
uvicorn[standard]==0.30.1
prometheus-client==0.20.0
pydantic==2.7.4
websockets==12.0

# Schema / data contracts (uniqueness feature: Avro + Schema Registry)
fastavro==1.9.4

# Data simulation & validation (synthetic dataset, offline checks)
pandas==2.2.2
numpy==1.26.4

# Utilities
python-dotenv==1.0.1
httpx==0.27.0

# Dev / testing
pytest==8.2.2
pytest-asyncio==0.23.7
